# Temporarily disabled while debugging Pagefind search.
# The previous strict Content-Security-Policy was likely blocking the
# WebAssembly module that Pagefind uses for on-site search.
# We'll re-add security headers once search is confirmed working.
